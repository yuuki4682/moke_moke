<% if obj.errors.any? %>
  <div class="alert alert-danger m-4">
    <h5>以下のエラーが発生しました。</h5>
    <ul>
      <% obj.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>