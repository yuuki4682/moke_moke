<%= form_with model: @work do |f| %>
  <%= f.text_field :title %>
  <%= f.text_area :caption %>
  <%= f.file_field :main_image, accept: "image/*" %>
  <%= f.file_field :sub_images, accept: "image/*", multiple: true %>
  <%= f.submit %>
<% end %>